---
title: "마크다운 스타일링 가이드"
date: "2026-02-13"
description: "이 블로그에서 지원하는 마크다운 문법과 스타일링을 확인합니다."
tags: ["markdown", "blog", "guide"]
---

# 마크다운 스타일링 가이드

이 포스트에서는 블로그에서 지원하는 다양한 마크다운 문법을 소개합니다.

## 텍스트 스타일

**굵은 텍스트**, *기울임 텍스트*, ~~취소선~~, 그리고 `인라인 코드`를 사용할 수 있습니다.

[링크](https://github.com)도 자연스럽게 렌더링됩니다.

## 목록

### 순서 없는 목록

- 첫 번째 항목
- 두 번째 항목
  - 중첩된 항목
  - 또 다른 중첩 항목
- 세 번째 항목

### 순서 있는 목록

1. 프로젝트 설정
2. 컴포넌트 작성
3. 스타일링 적용
4. 배포

## 인용문

> 단순함은 궁극의 정교함이다.
> — *Leonardo da Vinci*

## 표

| 도구 | 언어 | 용도 |
|------|------|------|
| Next.js | TypeScript | 웹 프레임워크 |
| Tailwind | CSS | 스타일링 |
| MDX | Markdown | 콘텐츠 작성 |
| Shiki | JavaScript | 구문 강조 |

## 코드 블록

### JavaScript

```javascript title="array-methods.js"
const numbers = [1, 2, 3, 4, 5];

// Map
const doubled = numbers.map((n) => n * 2);
console.log(doubled); // [2, 4, 6, 8, 10]

// Filter
const evens = numbers.filter((n) => n % 2 === 0);
console.log(evens); // [2, 4]

// Reduce
const sum = numbers.reduce((acc, n) => acc + n, 0);
console.log(sum); // 15
```

### CSS

```css title="styles.css"
.container {
  max-width: 768px;
  margin: 0 auto;
  padding: 2rem 1rem;
}

@media (prefers-color-scheme: dark) {
  :root {
    --bg: #0d1117;
    --text: #e6edf3;
  }
}
```

### JSON

```json title="package.json"
{
  "name": "my-blog",
  "version": "1.0.0",
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0"
  }
}
```

## 수평선

아래는 수평선입니다:

---

## 이미지 대체 텍스트

마크다운에서는 이미지를 쉽게 삽입할 수 있습니다. 이 블로그에서는 최적화된 이미지 로딩을 지원합니다.

## 체크리스트 (GFM)

- [x] 블로그 프로젝트 생성
- [x] 마크다운 렌더링 구현
- [x] 다크 모드 지원
- [ ] RSS 피드 추가
- [ ] 검색 기능 구현
